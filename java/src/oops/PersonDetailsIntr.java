package oops;
/*
    for interface ,cannot create object.
    so it can be used only in the inheritance
   interface's  should have only declaration.
            so interface's method definition shuld be in child class.
 */
public interface PersonDetailsIntr {
    public void printMessage();//only declaration.no definition.

    public void printMessage1();//only declaration.no definition.

}
